health_check:
  get:
    tags: [Health]
    summary: Check service health
    responses:
      "200":
        description: Service is healthy
        content:
          application/json:
            schema:
              $ref: ../schemas/common.yaml#/HealthResponse
